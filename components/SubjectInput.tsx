import React from 'react';

// This component is deprecated as functionality has been merged into TeacherInput.
// Kept file to prevent import errors during hot reload if references exist, but logic is removed.
export const SubjectInput: React.FC<any> = () => {
  return null;
};